# Module Reference

Archived note.

This page is no longer the live source-tree inventory.

It was written before the public API cutover and before the old public roots
were retired.

For the current package and directory map, use [SRC_STRUCTURE.md](SRC_STRUCTURE.md).

For the current user-facing import story, use [docs/api/index.md](api/index.md).

If you need the exact live file tree, inspect `src/fuggers_py/` directly.
