# `fuggers_py.adapters`

Retired public namespace.

This path is no longer part of the public API.

Start with the domain-first modules for pricing, curves, and analytics. Use
the domain-first modules for public work.

Storage, JSON, SQLite, transport, and file helpers now live under the internal
`_adapters` and `_storage` packages.
