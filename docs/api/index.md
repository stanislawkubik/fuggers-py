# API guide

Start with the first-layer public packages.

These pages group the public API by top-level package so the docs stay easy to
scan and match the current public import story.

If you are new to the repo, read [Project Status](../STATUS.md) first. The API
surface is already broad, but some parts of it are still intentionally moving
before `1.x`.

Legacy and internal pages are kept outside this main guide on purpose.

```{toctree}
:maxdepth: 1

curves
vol_surfaces
bonds
rates
inflation
credit
funding
portfolio
```
